<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>TRIX - How It Works</title>
<style>
  * {margin:0; padding:0; box-sizing:border-box; font-family:'Poppins', sans-serif;}

  /* Background: dark with floating posts */
  body {
    min-height:100vh;
    background-color:#0d0d0d;
    color:#fff;
    overflow-x:hidden;
    position:relative;
  }

  /* Floating post elements */
  .floating-post {
    position:absolute;
    width:60px;
    height:80px;
    background:rgba(255,255,255,0.03);
    border-radius:12px;
    animation: floatUp linear infinite;
  }

  @keyframes floatUp {
    0% {transform: translateY(110vh) rotate(0deg);}
    100% {transform: translateY(-120vh) rotate(360deg);}
  }

  .container {
    max-width:1100px;
    margin:0 auto;
    padding:60px 20px;
    text-align:center;
    position:relative;
    z-index:1;
  }

  h1 {
    font-size:2.5rem;
    margin-bottom:50px;
    letter-spacing:2px;
  }

  /* Cards container */
  .cards-group {
    display:flex;
    flex-direction:column;
    gap:50px;
    align-items:center;
  }

  /* Row of cards */
  .cards-row {
    display:flex;
    flex-wrap:wrap;
    gap:30px;
    justify-content:center;
  }

  .card {
    background:rgba(255,255,255,0.05);
    border-radius:20px;
    padding:30px 20px;
    backdrop-filter:blur(12px);
    transition:transform 0.4s, box-shadow 0.4s;
    cursor:default;
    min-height:200px;
    flex:0 0 280px;
    display:flex;
    flex-direction:column;
    justify-content:flex-start;
    align-items:center;
  }

  .card:hover {
    transform: translateY(-10px) scale(1.03);
    box-shadow:0 15px 35px rgba(0,0,0,0.6);
  }

  .card h2 {
    font-size:1.4rem;
    margin-bottom:15px;
    color:#ffeaa7;
  }

  .card p {
    font-size:0.95rem;
    color:#ccc;
    text-align:left;
  }

  .card-icon {
    font-size:2.5rem;
    margin-bottom:15px;
    color:#cfcfcf;
    transition: transform 0.4s;
  }

  .card:hover .card-icon {
    transform: scale(1.2) rotate(10deg);
  }
</style>
</head>
<body>

<!-- Floating posts -->
<div class="floating-post" style="left:5%; animation-duration:20s;"></div>
<div class="floating-post" style="left:20%; animation-duration:22s;"></div>
<div class="floating-post" style="left:35%; animation-duration:18s;"></div>
<div class="floating-post" style="left:50%; animation-duration:25s;"></div>
<div class="floating-post" style="left:65%; animation-duration:20s;"></div>
<div class="floating-post" style="left:80%; animation-duration:24s;"></div>

<div class="container">
  <h1>See How TRIX Works</h1>

  <div class="cards-group">
    <!-- Steps 1-3 -->
    <div class="cards-row">
      <div class="card">
        <div class="card-icon">🔑</div>
        <h2>Step 1: Log In / Sign Up</h2>
        <p>Create an account or log in to start posting and exploring articles.</p>
      </div>

      <div class="card">
        <div class="card-icon">📝</div>
        <h2>Step 2: Create a Post</h2>
        <p>Add your article title, content, references, and any images to share with the community.</p>
      </div>

      <div class="card">
        <div class="card-icon">🔍</div>
        <h2>Step 3: Explore Articles</h2>
        <p>Browse posts from other authors, search for topics, and discover new ideas.</p>
      </div>
    </div>

    <!-- Steps 4-5 centered -->
    <div class="cards-row">
      <div class="card">
        <div class="card-icon">⭐</div>
        <h2>Step 4: Interact</h2>
        <p>Like, comment, or bookmark articles. Support authors or subscribe for premium content.</p>
      </div>

      <div class="card">
        <div class="card-icon">📁</div>
        <h2>Step 5: Manage Portfolio</h2>
        <p>Organize your posts, track analytics, and showcase your work professionally.</p>
      </div>
    </div>
  </div>

</div>

</body
